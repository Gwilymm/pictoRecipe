{# Picto search partial (OpenFoodFacts) - placed inside pictogram templates folder #}
<div data-controller="picto-search" class="space-y-3">
    {# The name input will be rendered by the form and must have data-picto-search-target="input" #}
    <div class="w-full">
        <div data-picto-search-target="results" class="w-full"></div>
    </div>
</div>

{# Note: the form should render the name input with: data-picto-search-target="input" so the controller can bind to it. #}
