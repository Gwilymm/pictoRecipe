{% extends 'base.html.twig' %}

{% block title %}Modifier étape
{% endblock %}

{% block body %}
	<h1 class="text-3xl font-bold mb-6">Modifier l'étape</h1>

	<div class="mb-4">
		{{ include('step/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
	</div>

	<a href="{{ path('app_step_index') }}" class="btn btn-outline">Retour à la liste</a>

	<div class="mt-4">
		{{ include('step/_delete_form.html.twig') }}
	</div>
{% endblock %}
